<h3>Wishlist</h3>

<p>Total: <%=@wish_count.count%></p>

<%@owned.each do |y|%>
    <%if @ownership.find_by(card_id: y.id).wishlist?%>
        <a href="/cards/<%=y.id%>"><%= image_tag(y.image.thumb.url) %></a>
    <%end%>
<%end%>

<br><a href="/">Back</a>